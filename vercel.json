{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd shared && npm install && cd ../frontend && npm install && VITE_API_URL=https://hyt-backend.fly.dev/api npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skipping default install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
